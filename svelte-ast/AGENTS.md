Instructions for Codex agents in this repo:

- Use `bun` for installs and running scripts (not `npm`).
- Compilation must finish without warnings or errors.
- Any Markdown files you generate must wrap lines to < 80 chars.
